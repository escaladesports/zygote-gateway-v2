service: serverless-aws-boilerplate

provider:
  name: aws
  runtime: nodejs6.10
  region: us-east-1

functions:
  hello:
    handler: handler.hello
    events:
      - http:
        path: handler
        method: post
        cors: true

plugins:
  - serverless-plugin-optimize